# Unit tests (no API)

Fast tests for `sts_test_framework.runners.functional` helpers (`_pagination_pair_check`, `_check_basic_shape` edge cases) using mocked `APIResponse` objects only.

Run only this folder: `pytest tests/unit/ -v`
